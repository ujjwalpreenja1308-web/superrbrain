{
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @covable/shared build && pnpm --filter @covable/frontend build",
  "outputDirectory": "apps/frontend/dist"
}
